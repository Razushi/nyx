<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet href="pretty-feed-v3.xsl" type="text/xsl"?>
<rss version="2.0">
  <channel>
    <title>NotAShelf&#39;s Notes</title>
    <link>https://notashelf.github.io/nyx</link>
    <description>NotAShelf&#39;s Notes on Various Topics</description>
    <item>
      <title>cheatsheet</title>
      <link>cheatsheet.html</link>
      <description>&lt;h1 id=&#34;cheat-sheet&#34;&gt;Cheat sheet&lt;/h1&gt;&#xA;&lt;h2 id=&#34;show-gc-roots&#34;&gt;Show GC roots&lt;/h2&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;nix-store --gc --print-roots | grep -v &amp;quot;&amp;lt;hostName&amp;gt;&amp;quot; | column -t | sort -k3 -k1&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;list-all-packages&#34;&gt;List all packages&lt;/h2&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;nix-store -q --requisites /run/current-system | cut -d- -f2- | sort | uniq&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;You can add a &lt;code&gt;wc -l&lt;/code&gt; at the end of the above command, but that will not be an accurate representation of&lt;br&gt;&#xA;your package count, as the same package can be repeated with different versions.&lt;/p&gt;&#xA;&lt;h2 id=&#34;find-biggest-packages&#34;&gt;Find biggest packages&lt;/h2&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;nix path-info -hsr /run/current-system/ | sort -hrk2 | head -n10&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;find-biggest-closures-packages-including-dependencies&#34;&gt;Find biggest closures (packages including dependencies)&lt;/h2&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;nix path-info -hSr /run/current-system/ | sort -hrk2 | head -n10&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;show-package-dependencies-as-tree&#34;&gt;Show package dependencies as tree&lt;/h2&gt;&#xA;&lt;blockquote&gt;&#xA;&lt;p&gt;Assuming &lt;code&gt;hello&lt;/code&gt; is in PATH&lt;/p&gt;&#xA;&lt;/blockquote&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;nix-store -q --tree $(realpath $(which hello))&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;show-package-dependencies-including-size&#34;&gt;Show package dependencies including size&lt;/h2&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;nix path-info -hSr nixpkgs#hello&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;show-the-things-that-will-change-on-reboot&#34;&gt;Show the things that will change on reboot&lt;/h2&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-shell&#34;&gt;diff &amp;lt;(nix-store -qR /run/current-system) &amp;lt;(nix-store -qR  /run/booted-system)&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;</description>
      <pubDate>Thu, 04 Apr 2024 02:27:18 +0000</pubDate>
    </item>
    <item>
      <title>yubikey-todo</title>
      <link>yubikey-todo.html</link>
      <description>&lt;h1 id=&#34;todo&#34;&gt;TODO&lt;/h1&gt;&#xA;&lt;!-- raw HTML omitted --&gt;&#xA;&lt;h2 id=&#34;resources&#34;&gt;Resources&lt;/h2&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;&lt;a href=&#34;https://superuser.com/questions/1628782/gpg-signing-failed-no-pinentry&#34;&gt;https://superuser.com/questions/1628782/gpg-signing-failed-no-pinentry&lt;/a&gt;&lt;/li&gt;&#xA;&lt;li&gt;&lt;a href=&#34;https://superuser.com/questions/397149/can-you-gpg-sign-old-commits&#34;&gt;https://superuser.com/questions/397149/can-you-gpg-sign-old-commits&lt;/a&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
      <pubDate>Thu, 04 Apr 2024 02:27:18 +0000</pubDate>
    </item>
    <item>
      <title>using-headscale</title>
      <link>using-headscale.html</link>
      <description>&lt;h1 id=&#34;notes-for-11th-of-november-2023&#34;&gt;Notes for 11th of November, 2023&lt;/h1&gt;&#xA;&lt;p&gt;Today&#39;s main attraction is the Headscale setup on my VPS running NixOS, which&lt;br&gt;&#xA;I&#39;ve finally came around to self-host.&lt;/p&gt;&#xA;&lt;p&gt;There has been much talk about this new product called Tailscale recently&lt;br&gt;&#xA;around the web, especially in the last few years. Tailscale is a VPN&lt;br&gt;&#xA;service that makes the devices and applications we own accessible anywhere&lt;br&gt;&#xA;using the open source WireGuard protocol to establish encrypted point-to-point&lt;br&gt;&#xA;connections. I have been using Tailscale for a while now, but in an effort&lt;br&gt;&#xA;to move all of my services to self-owned hardware some of my services have&lt;br&gt;&#xA;been moved over to my NixOS server over time.&lt;/p&gt;&#xA;&lt;p&gt;Many of Tailscale’s components are open-source, especially its clients, but&lt;br&gt;&#xA;the server remains closed-source. Tailscale is a SaaS product and monetization&lt;br&gt;&#xA;naturally is a big concern, however, we care more about controlling our own data&lt;br&gt;&#xA;than their attempts of monetization.&lt;/p&gt;&#xA;&lt;p&gt;This is where the (very appropriately named) Headscale comes in; Headscale is&lt;br&gt;&#xA;an open-source, self-hosted implementation of the Tailscale control server. The&lt;br&gt;&#xA;configuration is extremely straightforward, as Headscale will handle everything&lt;br&gt;&#xA;for us.&lt;/p&gt;&#xA;&lt;h2 id=&#34;running-headscale&#34;&gt;Running Headscale&lt;/h2&gt;&#xA;&lt;p&gt;Below is a simple configuration for the Headscale module of NixOS.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;services = let&#xA;  domain = &amp;quot;example.com&amp;quot;;&#xA;in {&#xA;  headscale = {&#xA;    enable = true;&#xA;    address = &amp;quot;0.0.0.0&amp;quot;;&#xA;    port = 8085;&#xA;&#xA;    settings = {&#xA;      server_url = &amp;quot;https://tailscale.${domain}&amp;quot;;&#xA;&#xA;      dns_config = {&#xA;        override_local_dns = true;&#xA;        base_domain = &amp;quot;${domain}&amp;quot;;&#xA;        magic_dns = true;&#xA;        domains = [&amp;quot;tailscale.${domain}&amp;quot;];&#xA;        nameservers = [&#xA;          &amp;quot;9.9.9.9&amp;quot; # no cloudflare, nice&#xA;        ];&#xA;      };&#xA;&#xA;      ip_prefixes = [&#xA;        &amp;quot;100.64.0.0/10&amp;quot;&#xA;        &amp;quot;fd7a:115c:a1e0::/48&amp;quot;&#xA;      ];&#xA;    };&#xA;  };&#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h2 id=&#34;using-headscale&#34;&gt;Using Headscale&lt;/h2&gt;&#xA;&lt;p&gt;We must first create a user, which we can do with&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-console&#34;&gt;headscale users create myUser&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;Then on the machine that will be our client, we need to login.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-console&#34;&gt;tailscale up --login-server tailscale.example.com # replace this URL with your own as configured abovea&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;Followed by registering the machine.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-console&#34;&gt;# machine key will be obtained visiting the URL that is returned from the above command&#xA;headscale --user myUser nodes register --key &amp;lt;MACHINE_KEY&amp;gt;&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;And finally logging into your Tailnet using the URL and your machine key.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-console&#34;&gt;tailscale up --login-server https://tailscale.example.com --authkey &amp;lt;YOUR_AUTH_KEY&amp;gt;&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;And all done! Now try connecting to one of your machines using the hostname now&lt;br&gt;&#xA;to test if the connection is actually working. If anything goes wrong, make&lt;br&gt;&#xA;sure to check your DNS settings: remember, it&#39;s always the DNS.&lt;/p&gt;&#xA;</description>
      <pubDate>Sat, 11 Nov 2023 00:00:00 +0000</pubDate>
    </item>
    <item>
      <title>openssh-custom-port</title>
      <link>openssh-custom-port.html</link>
      <description>&lt;h1 id=&#34;notes-for-14th-of-july-2023&#34;&gt;Notes for 14th of July, 2023&lt;/h1&gt;&#xA;&lt;p&gt;My VPS, which hosts some of my infrastructure, has been running NixOS&lt;br&gt;&#xA;for a while now. Although weak, I use it for distributed builds alongside the&lt;br&gt;&#xA;rest of my NixOS machines on a Tailscale network.&lt;/p&gt;&#xA;&lt;p&gt;This server, due to it hosting my infrastructure that communicates with the&lt;br&gt;&#xA;rest of the internet (i.e my mailserver), is somewhat responsive to queries&lt;br&gt;&#xA;from the public - which includes &lt;em&gt;very&lt;/em&gt; agressive portscans (thanks, skiddies!)&lt;/p&gt;&#xA;&lt;p&gt;To mitigate that, I have decided to change the ssh port from the default &lt;strong&gt;22&lt;/strong&gt;&lt;br&gt;&#xA;to something different. While this is not exactly a pancea, it helps alleviate&lt;br&gt;&#xA;the insane log spam I get from failed ssh requests.&lt;/p&gt;&#xA;&lt;h2 id=&#34;the-openssh-configuration&#34;&gt;The OpenSSH Configuration&lt;/h2&gt;&#xA;&lt;p&gt;First thing we&#39;ve done is to configure openssh to listen on the new port on&lt;br&gt;&#xA;your server configuration&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;services.openssh = {&#xA;  ports = [2222];&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;With this set, openssh on the server will now be listening on the port &lt;strong&gt;2222&lt;/strong&gt;&lt;br&gt;&#xA;instead of the default &lt;strong&gt;22&lt;/strong&gt;. For the changes to take effect after a&lt;br&gt;&#xA;rebuild, you might need to run &lt;code&gt;systemctl restart sshd.socket&lt;/code&gt;.&lt;/p&gt;&#xA;&lt;p&gt;Then we want to configure our client to use the correct port for our server&lt;br&gt;&#xA;instead of the default &lt;strong&gt;22&lt;/strong&gt;.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;programs.ssh.extraConfig = &#39;&#39;&#xA;    Host nix-builder&#xA;      HostName nix-builder-hostname # if you are using Tailscale, this can just be the hostname of a device on your Tailscale network&#xA;&#x9;  Port 2222&#xA;&#39;&#39;;&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;And done, that is all for the ssh side of things. Next up, we need to configure&lt;br&gt;&#xA;out builder to use the correct host.&lt;/p&gt;&#xA;&lt;h2 id=&#34;nix-builder-configuration&#34;&gt;Nix Builder Configuration&lt;/h2&gt;&#xA;&lt;p&gt;Assuming you already have a remote builder configured, you will only need to&lt;br&gt;&#xA;patch the &lt;code&gt;hostName&lt;/code&gt; with the one on your &lt;code&gt;openssh.extraConfig&lt;/code&gt;.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;nix.buildMachines = [{&#xA;    hostName = &amp;quot;nix-builder-hostname&amp;quot;;&#xA;    sshUser = &amp;quot;nix-builder&amp;quot;;&#xA;    sshKey = &amp;quot;/path/to/key&amp;quot;;&#xA;    systems = [&amp;quot;x86_64-linux&amp;quot;];&#xA;    maxJobs = 2;&#xA;    speedFactor = 2;&#xA;    supportedFeatures = [&amp;quot;kvm&amp;quot;];&#xA;}];&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;If you have added the correct &lt;code&gt;hostName&lt;/code&gt; and &lt;code&gt;sshUser&lt;/code&gt;, the builder will be&lt;br&gt;&#xA;picked up automatically on the next rebuild.&lt;/p&gt;&#xA;&lt;h3 id=&#34;home-manager&#34;&gt;Home-Manager&lt;/h3&gt;&#xA;&lt;p&gt;If you are using Home-Manager, you might also want to configure your&lt;br&gt;&#xA;declarative ~/.config/ssh/config to use the new port. That can be achieved&lt;br&gt;&#xA;through &lt;code&gt;programs.ssh.matchBlocks&lt;/code&gt; option under Home-Manager&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;programs.ssh.matchBlocks = {&#xA;  &amp;quot;builder&amp;quot; = {&#xA;    hostname = &amp;quot;nix-builder-hostname&amp;quot;;&#xA;    user = &amp;quot;nix-builder&amp;quot;;&#xA;    identityFile = &amp;quot;~/.ssh/builder-key&amp;quot;;&#xA;    port = 2222;&#xA;  };&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;And that will be all. You are ready to use your new non-default port, mostly&lt;br&gt;&#xA;safe from port scanners.&lt;/p&gt;&#xA;</description>
      <pubDate>Fri, 14 Jul 2023 00:00:00 +0000</pubDate>
    </item>
    <item>
      <title>extended-nixpkgs</title>
      <link>extended-nixpkgs.html</link>
      <description>&lt;h1 id=&#34;notes-for-7th-of-june-2023&#34;&gt;Notes for 7th of June, 2023&lt;/h1&gt;&#xA;&lt;p&gt;Those are my notes on extending nixpkgs with your own functions and&lt;br&gt;&#xA;abstractions. There may be other ways of doing it, but this is the one I find&lt;br&gt;&#xA;to be most ergonomic.&lt;/p&gt;&#xA;&lt;h2 id=&#34;what-is-nixpkgslib&#34;&gt;What is &lt;code&gt;nixpkgs.lib&lt;/code&gt;&lt;/h2&gt;&#xA;&lt;p&gt;In the context of the Nix package manager and NixOS, &lt;code&gt;nixpkgs.lib&lt;/code&gt; refers to&lt;br&gt;&#xA;a module within the Nixpkgs repository. The &lt;code&gt;nixpkgs.lib&lt;/code&gt; module provides a&lt;br&gt;&#xA;set of utility functions and definitions that are commonly used across the&lt;br&gt;&#xA;Nixpkgs repository. It contains various helper functions and abstractions that&lt;br&gt;&#xA;make it easier to write Nix expressions and define packages. We often use those&lt;br&gt;&#xA;functions to simplify our configurations and the nix package build processes.&lt;/p&gt;&#xA;&lt;h2 id=&#34;why-would-you-need-to-extend-nixpkgslib&#34;&gt;Why would you need to extend &lt;code&gt;nixpkgs.lib&lt;/code&gt;&lt;/h2&gt;&#xA;&lt;p&gt;While the library functions provided by nixpkgs is quite extensive and usually&lt;br&gt;&#xA;suits my needs, I sometimes feel the need to define my own function or wrap an&lt;br&gt;&#xA;existing function to complete a task. Normally we can handle the process of a&lt;br&gt;&#xA;function inside a simple &lt;code&gt;let in&lt;/code&gt; and be well off, but there may be times you&lt;br&gt;&#xA;need to re-use the existing function across your configuration file.&lt;/p&gt;&#xA;&lt;p&gt;In such times, you might want to either write your own lib and inherit it at&lt;br&gt;&#xA;the source of your &lt;code&gt;flake.nix&lt;/code&gt; to then inherit them across your configuration.&lt;/p&gt;&#xA;&lt;p&gt;Today&#39;s notes document the process of doing exactly that.&lt;/p&gt;&#xA;&lt;h2 id=&#34;extending-nixpkgslib&#34;&gt;Extending &lt;code&gt;nixpkgs.lib&lt;/code&gt;&lt;/h2&gt;&#xA;&lt;p&gt;I find the easiest way of extending nixpkgs.lib to be using an overlay.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;# lib/default.nix&#xA;{&#xA;  nixpkgs,&#xA;  lib,&#xA;  inputs,&#xA;  ...&#xA;}: nixpkgs.lib.extend (&#xA;    final: prev: {&#xA;        # your functions go here&#xA;    }&#xA;  )&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;The above structure takes the existing &lt;code&gt;lib&lt;/code&gt; from &lt;code&gt;nixpkgs&lt;/code&gt;, and appends your&lt;br&gt;&#xA;own configurations to it. You may then import this library in your &lt;code&gt;flake.nix&lt;/code&gt;&lt;br&gt;&#xA;to pass it to other imports and definitions.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;# flake.nix&#xA;flake = let&#xA;    # extended nixpkgs lib, contains my custom functions&#xA;    lib = import ./lib {inherit nixpkgs lib inputs;};&#xA;in {&#xA;    # entry-point for nixos configurations&#xA;    nixosConfigurations = import ./hosts {inherit nixpkgs self lib;};&#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;In this example (see my &lt;code&gt;flake.nix&lt;/code&gt; for the actual implementation) I import my&lt;br&gt;&#xA;extended lib from &lt;code&gt;lib/default.nix&lt;/code&gt;, where I defined the overlay. I then pass&lt;br&gt;&#xA;the extended lib to my &lt;code&gt;nixosConfiguratiıns&lt;/code&gt;, which is an entry-point for all&lt;br&gt;&#xA;of my NixOS configurations. As such, I am able to re-use my own utility&lt;br&gt;&#xA;functions across my system as I see fit.&lt;/p&gt;&#xA;&lt;p&gt;The problem with this approach is that it may be confusing for other people&lt;br&gt;&#xA;reviewing your configuration. With this approach, &lt;code&gt;lib.customFunction&lt;/code&gt; looks&lt;br&gt;&#xA;identical to any lib function, which may lead to people thinking the function&lt;br&gt;&#xA;exists in nixpkgs itself while it is only provided by your configuration. The&lt;br&gt;&#xA;solution for that is simple though, instead of extending &lt;code&gt;nixpkgs.lib&lt;/code&gt;, you may&lt;br&gt;&#xA;define your own lib that does not inherit from &lt;code&gt;nixpkgs.lib&lt;/code&gt; and only contains&lt;br&gt;&#xA;your functions. The process would be similar, and you would not need to define&lt;br&gt;&#xA;an overlay.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;# flake.nix&#xA;flake = let&#xA;    # extended nixpkgs lib, contains my custom functions&#xA;    lib&#39; = import ./lib {inherit nixpkgs lib inputs;};&#xA;in {&#xA;    # entry-point for nixos configurations&#xA;    nixosConfigurations = import ./hosts {inherit nixpkgs self lib&#39;;};&#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;where your &lt;code&gt;lib/default.nix&lt;/code&gt; looks like&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;# lib/default.nix&#xA;{&#xA;  nixpkgs,&#xA;  lib,&#xA;  inputs,&#xA;  ...&#xA;}: {&#xA;    # your functions here&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;You can find a real life example of the alternative approach in&lt;br&gt;&#xA;my &lt;a href=&#34;https://github.com/NotAShelf/neovim-flake/blob/main/lib/stdlib-extended.nix&#34;&gt;neovim-flake&#39;s lib&lt;/a&gt;.&lt;/p&gt;&#xA;</description>
      <pubDate>Wed, 07 Jun 2023 00:00:00 +0000</pubDate>
    </item>
    <item>
      <title>packaging-nextjs-webapps</title>
      <link>packaging-nextjs-webapps.html</link>
      <description>&lt;h1 id=&#34;notes-for-21st-of-june-2023&#34;&gt;Notes for 21st of June, 2023&lt;/h1&gt;&#xA;&lt;p&gt;Recenty I have had to go through the misfortune of hosting some websites&lt;br&gt;&#xA;written with &lt;em&gt;NextJS&lt;/em&gt; on my VPS running NixOS, this note entry shall document&lt;br&gt;&#xA;my experience and the &amp;quot;easy&amp;quot; path I have chosen.&lt;/p&gt;&#xA;&lt;h2 id=&#34;packaging&#34;&gt;Packaging&lt;/h2&gt;&#xA;&lt;p&gt;The websites I hosted were of two variety: those statically exported, and&lt;br&gt;&#xA;those that cannot be statically exported.&lt;/p&gt;&#xA;&lt;h3 id=&#34;statically-exported-webapps&#34;&gt;Statically Exported Webapps&lt;/h3&gt;&#xA;&lt;p&gt;Statically exported ones are easy to package, because it is a matter of&lt;br&gt;&#xA;running &lt;code&gt;npm build&lt;/code&gt; (or whatever your build script is) with the following&lt;br&gt;&#xA;NextJS settings&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;// next.config.js&#xA;module.exports = {&#xA;  distDir: &amp;quot;dist&amp;quot;, // an artitrary path for your export&#xA;  output: &amp;quot;export&amp;quot;,&#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;This will export a static website with a bunch of html files that you can&lt;br&gt;&#xA;then serve with nodePackages.serve or a webserver like nginx or apache.&lt;br&gt;&#xA;And that is the end of your worries for a statically exported website! No&lt;br&gt;&#xA;headache, just write a simple derivation, such as the one below&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;# default.nix&#xA;{&#xA;  buildNpmPackage,&#xA;  pkg-config,&#xA;  python3,&#xA;  ...&#xA;}:&#xA;buildNpmPackage {&#xA;  pname = &amp;quot;your-website&amp;quot;;&#xA;  version = &amp;quot;0.1&amp;quot;;&#xA;&#xA;  src = ./.;&#xA;  # needs to be updated everytime you update npm dependencies&#xA;  npmDepsHash = &amp;quot;sha256-some-hash&amp;quot;;&#xA;  # some npm packages may need to be built from source, because nodejs is a *terrible* ecosystem&#xA;  nativeBuildInputs = [pkg-config python3];&#xA;&#xA; # move exported website to $out&#xA; postInstall = &#39;&#39;&#xA;    cp -rf dist/* $out&#xA;  &#39;&#39;;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;and serve its path with a simple tool after building the derivation, I find&lt;br&gt;&#xA;nginx to be awfully convenient for doing so, but you may choose caddy if you&lt;br&gt;&#xA;prefer.&lt;/p&gt;&#xA;&lt;h3 id=&#34;webapps-that-cannot-be-statically-exported&#34;&gt;Webapps that cannot be statically exported&lt;/h3&gt;&#xA;&lt;p&gt;If your website depends on API routes for some reasons, then Next will not&lt;br&gt;&#xA;allow you to do static export. Which means you need to run &lt;code&gt;next start&lt;/code&gt; in&lt;br&gt;&#xA;some shape or form. While a systemd service is certainly a way of doing it&lt;br&gt;&#xA;(one that I do not recommend), a oci container works as well if not better.&lt;/p&gt;&#xA;&lt;p&gt;You can write a &amp;quot;simple&amp;quot; docker image for your oci container to use, such as&lt;br&gt;&#xA;the one below&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;# dockerImage.nix&#xA;{&#xA;  pkgs,&#xA;  inputs,&#xA;  ...&#xA;}: {&#xA;  dockerImage = pkgs.dockerTools.buildImage {&#xA;    config = {&#xA;      WorkingDir = &amp;quot;/your-website&amp;quot;;&#xA;      Cmd = [&amp;quot;npm&amp;quot; &amp;quot;run&amp;quot; &amp;quot;serve&amp;quot;];&#xA;    };&#xA;&#xA;    name = &amp;quot;your-website&amp;quot;;&#xA;    tag = &amp;quot;latest&amp;quot;;&#xA;&#xA;    fromImage = pkgs.dockerTools.buildImage {&#xA;      name = &amp;quot;node&amp;quot;;&#xA;      tag = &amp;quot;18-alpine&amp;quot;;&#xA;    };&#xA;&#xA;    copyToRoot = pkgs.buildEnv {&#xA;      name = &amp;quot;image-root&amp;quot;;&#xA;&#xA;      paths = with pkgs; [&#xA;        # this package is called from a flake.nix alongside the derivation for the website&#xA;        inputs.self.packages.${pkgs.system}.your-website&#xA;        nodejs&#xA;        bash&#xA;      ];&#xA;&#xA;      pathsToLink = [&#xA;        &amp;quot;/bin&amp;quot;&#xA;        &amp;quot;/your-website&amp;quot;&#xA;      ];&#xA;    };&#xA;  };&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;Then, configure oci-containers module option to pick up the Docker image that&lt;br&gt;&#xA;you have built. This is a simplified version of my VPS&#39; container setup.&lt;br&gt;&#xA;An example can be found in my &lt;a href=&#34;https://github.com/NotAShelf/nyx/blob/a9e129663ac91302f2fd935351a71cbbd2832f64/modules/core/roles/server/system/services/mkm.nix&#34;&gt;server module&lt;/a&gt;&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;virtualisation.oci-containers = {&#xA;  backend = &amp;quot;podman&amp;quot;;&#xA;  containers = {&#xA;    &amp;quot;website-container&amp;quot; = {&#xA;      autoStart = true;&#xA;      ports = [&#xA;        &amp;quot;3000:3000&amp;quot; # bind container&#39;s port 3000 to the outside port 3000 for NextJS&#xA;      ];&#xA;&#xA;      extraOptions = [&amp;quot;--network=host&amp;quot;];&#xA;&#xA;      image = &amp;quot;your-website&amp;quot;;&#xA;      imageFile = inputs.website-flake.packages.${pkgs.system}.dockerImage;&#xA;    };&#xA;  };&#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;After a rebuild, your system will provision the container and start it on&lt;br&gt;&#xA;port &lt;strong&gt;3000&lt;/strong&gt;. You can access it with &lt;code&gt;your-server-ip:3000&lt;/code&gt; in your&lt;br&gt;&#xA;browser, and even configure nginx to set up a reverse proxy to assign&lt;br&gt;&#xA;your domain.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-conf&#34;&gt;&amp;quot;example.com&amp;quot; = {&#xA;  locations.&amp;quot;/&amp;quot;.proxyPass = &amp;quot;http://127.0.0.1:3000&amp;quot;;&#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;This will assign your domain to your webserver, and allow outside&lt;br&gt;&#xA;visitors to view your &amp;quot;awesome&amp;quot; NextJS webapp.&lt;/p&gt;&#xA;</description>
      <pubDate>Sun, 21 May 2023 00:00:00 +0000</pubDate>
    </item>
    <item>
      <title>impermanence</title>
      <link>impermanence.html</link>
      <description>&lt;h1 id=&#34;notes-for-14th-of-march-2023&#34;&gt;Notes for 14th of March, 2023&lt;/h1&gt;&#xA;&lt;p&gt;Today was the day I finally got to setting up both &amp;quot;erase your darlings&amp;quot;&lt;br&gt;&#xA;and proper disk encryption. This general setup concept utilizes NixOS&#39;&lt;br&gt;&#xA;ability to boot off of a disk that contains only &lt;code&gt;/nix&lt;/code&gt; and &lt;code&gt;/boot&lt;/code&gt;, linking&lt;br&gt;&#xA;appropriate devices and blocks during the boot process and deleting all state&lt;br&gt;&#xA;that programs may have left over my system.&lt;/p&gt;&#xA;&lt;p&gt;The end result, for me, was a fully encrypted that uses btrfs&lt;br&gt;&#xA;snapshots to restore &lt;code&gt;/&lt;/code&gt; to its original state on each boot.&lt;/p&gt;&#xA;&lt;h2 id=&#34;resources&#34;&gt;Resources&lt;/h2&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;&lt;a href=&#34;https://discourse.nixos.org/t/impermanence-vs-systemd-initrd-w-tpm-unlocking/25167&#34;&gt;This discourse post&lt;/a&gt;&lt;/li&gt;&#xA;&lt;li&gt;&lt;a href=&#34;https://elis.nu/blog/2020/06/nixos-tmpfs-as-home&#34;&gt;This blog post&lt;/a&gt;&lt;/li&gt;&#xA;&lt;li&gt;&lt;a href=&#34;https://guekka.github.io/nixos-server-1/&#34;&gt;This other blog post&lt;/a&gt;&lt;/li&gt;&#xA;&lt;li&gt;&lt;a href=&#34;https://mt-caret.github.io/blog/posts/2020-06-29-optin-state.html&#34;&gt;And this post that the previous post is based on&lt;/a&gt;&lt;/li&gt;&#xA;&lt;li&gt;&lt;a href=&#34;https://github.com/nix-community/impermanence&#34;&gt;Impermanence&lt;/a&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;h2 id=&#34;the-actual-set-up-and-reproduction-steps&#34;&gt;The actual set-up (and reproduction steps)&lt;/h2&gt;&#xA;&lt;p&gt;I&#39;ve had to go through a few guides before I could figure out a set up that I&lt;br&gt;&#xA;really like. The final decision was that I would have an encrypted disk that&lt;br&gt;&#xA;restores itself to its former state during boot. Is it fast? Absolutely not.&lt;br&gt;&#xA;But it sure as hell is cool. And stateless!&lt;/p&gt;&#xA;&lt;p&gt;To return the root (and only the root) we use a systemd service that fires&lt;br&gt;&#xA;shortly after the disk is encrypted but before the root is actually mounted.&lt;br&gt;&#xA;That way, we can unlock the disk, restore the disk to its pristine state&lt;br&gt;&#xA;using the snapshot we have taken during installation and mount the root to&lt;br&gt;&#xA;go on with our day.&lt;/p&gt;&#xA;&lt;h3 id=&#34;reproduction-steps&#34;&gt;Reproduction steps&lt;/h3&gt;&#xA;&lt;h4 id=&#34;partitioning&#34;&gt;Partitioning&lt;/h4&gt;&#xA;&lt;p&gt;First you want to format your disk. If you are really comfortable with&lt;br&gt;&#xA;bringing parted to your pre-formatted disks, by all means feel free to skip&lt;br&gt;&#xA;this section. I, however, choose to format a fresh disk.&lt;/p&gt;&#xA;&lt;p&gt;Start by partitioning the sections of our disk (sda1, sda2 and sda3)&lt;br&gt;&#xA;&lt;em&gt;Device names might change if you&#39;re using a nvme disk, i.e nvme0p1.&lt;/em&gt;&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;# Set the disk name to make it easier&#xA;DISK=/dev/sda # replace this with the name of the device you are using&#xA;&#xA;# set up the boot partition&#xA;parted &amp;quot;$DISK&amp;quot; -- mklabel gpt&#xA;parted &amp;quot;$DISK&amp;quot; -- mkpart ESP fat32 1MiB 1GiB&#xA;parted &amp;quot;$DISK&amp;quot; -- set 1 boot on&#xA;&#xA;mkfs.vfat -n BOOT &amp;quot;$DISK&amp;quot;1&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;# set up the swap partition&#xA;parted &amp;quot;$DISK&amp;quot; -- mkpart Swap linux-swap 1GiB 9GiB&#xA;mkswap -L SWAP &amp;quot;$DISK&amp;quot;2&#xA;swapon &amp;quot;$DISK&amp;quot;2&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;&lt;em&gt;I do in fact use swap in the civilized year of 2023&lt;sup id=&#34;fnref:1&#34;&gt;&lt;a href=&#34;#fn:1&#34; class=&#34;footnote-ref&#34; role=&#34;doc-noteref&#34;&gt;1&lt;/a&gt;&lt;/sup&gt;. If I were a little&lt;br&gt;&#xA;more advanced, and if I did not disable hibernation due to overly-hardened&lt;br&gt;&#xA;kernel parameters, I would also be encrypting the swap to secure the hibernates...&lt;br&gt;&#xA;but that is &lt;em&gt;currently&lt;/em&gt; out of my scope. You may find this desirable, however, I&lt;br&gt;&#xA;will not be providing instructions on that.&lt;/em&gt;&lt;/p&gt;&#xA;&lt;p&gt;Encrypt your partition, and open it to make it available under &lt;code&gt;/dev/mapper/enc&lt;/code&gt;.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;cryptsetup --verify-passphrase -v luksFormat &amp;quot;$DISK&amp;quot;3 # /dev/sda3&#xA;cryptsetup open &amp;quot;$DISK&amp;quot;3 enc&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;Now partition the encrypted device block.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;parted &amp;quot;$DISK&amp;quot; -- mkpart primary 9GiB 100%&#xA;mkfs.btrfs -L NIXOS /dev/mapper/enc&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;mount -t btrfs /dev/mapper/enc /mnt&#xA;&#xA;# First we create the subvolumes, those may differ as per your preferences&#xA;btrfs subvolume create /mnt/root&#xA;btrfs subvolume create /mnt/home&#xA;btrfs subvolume create /mnt/nix&#xA;btrfs subvolume create /mnt/persist # some people may choose to put /persist in /mnt/nix, I am not one of those people.&#xA;btrfs subvolume create /mnt/log&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;Now that we have created the btrfs subvolumes, it is time for the &lt;em&gt;readonly&lt;/em&gt;&lt;br&gt;&#xA;snapshot of the root subvolume.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;btrfs subvolume snapshot -r /mnt/root /mnt/root-blank&#xA;&#xA;# Make sure to unmount, or nixos-rebuild will try to remove /mnt and fail&#xA;umount /mnt&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;h4 id=&#34;mounting&#34;&gt;Mounting&lt;/h4&gt;&#xA;&lt;p&gt;After the subvolumes are created, we mount them with the options that we want.&lt;br&gt;&#xA;Ideally, on NixOS, you want the &lt;code&gt;noatime&lt;/code&gt; option &lt;sup id=&#34;fnref:2&#34;&gt;&lt;a href=&#34;#fn:2&#34; class=&#34;footnote-ref&#34; role=&#34;doc-noteref&#34;&gt;2&lt;/a&gt;&lt;/sup&gt; and zstd&lt;br&gt;&#xA;compression, especially on your &lt;code&gt;/nix&lt;/code&gt; partition.&lt;/p&gt;&#xA;&lt;p&gt;The following is my partition layout. If you have created any other subvolumes&lt;br&gt;&#xA;in the step above, you will also want to mount them here. Below setup assumes&lt;br&gt;&#xA;that you have been following the steps as is.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;# /&#xA;mount -o subvol=root,compress=zstd,noatime /dev/mapper/enc /mnt&#xA;&#xA;# /home&#xA;mkdir /mnt/home&#xA;mount -o subvol=home,compress=zstd,noatime /dev/mapper/enc /mnt/home&#xA;&#xA;# /nix&#xA;mkdir /mnt/nix&#xA;mount -o subvol=nix,compress=zstd,noatime /dev/mapper/enc /mnt/nix&#xA;&#xA;# /persist&#xA;mkdir /mnt/persist&#xA;mount -o subvol=persist,compress=zstd,noatime /dev/mapper/enc /mnt/persist&#xA;&#xA;# /var/log&#xA;mkdir -p /mnt/var/log&#xA;mount -o subvol=log,compress=zstd,noatime /dev/mapper/enc /mnt/var/log&#xA;&#xA;# do not forget to mount the boot partition&#xA;mkdir /mnt/boot&#xA;mount &amp;quot;$DISK&amp;quot;1 /mnt/boot&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;And finally let NixOS generate the hardware configuration.&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-bash&#34;&gt;nixos-generate-config --root /mnt&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;The genereated configuration will be available at &lt;code&gt;/mnt/etc/nixos&lt;/code&gt;.&lt;/p&gt;&#xA;&lt;p&gt;Before we move on, we need to add the &lt;code&gt;neededForBoot = true;&lt;/code&gt; to some mounted&lt;br&gt;&#xA;subvolumes in &lt;code&gt;hardware-configuration.nix&lt;/code&gt;. It will look something like this:&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;# Do not modify this file!  It was generated by ‘nixos-generate-config’&#xA;# and may be overwritten by future invocations.  Please make changes&#xA;# to /etc/nixos/configuration.nix instead.&#xA;{&#xA;  config,&#xA;  lib,&#xA;  pkgs,&#xA;  modulesPath,&#xA;  ...&#xA;}: {&#xA;  imports = [&#xA;    (modulesPath + &amp;quot;/installer/scan/not-detected.nix&amp;quot;)&#xA;  ];&#xA;&#xA;  boot.initrd.availableKernelModules = [&amp;quot;xhci_pci&amp;quot; &amp;quot;ahci&amp;quot; &amp;quot;usb_storage&amp;quot; &amp;quot;sd_mod&amp;quot; &amp;quot;rtsx_pci_sdmmc&amp;quot;];&#xA;  boot.initrd.kernelModules = [];&#xA;  boot.kernelModules = [&amp;quot;kvm-intel&amp;quot;];&#xA;  boot.extraModulePackages = [];&#xA;&#xA;  fileSystems.&amp;quot;/&amp;quot; = {&#xA;    device = &amp;quot;/dev/disk/by-uuid/b79d3c8b-d511-4d66-a5e0-641a75440ada&amp;quot;;&#xA;    fsType = &amp;quot;btrfs&amp;quot;;&#xA;    options = [&amp;quot;subvol=root&amp;quot;];&#xA;  };&#xA;&#xA;  boot.initrd.luks.devices.&amp;quot;enc&amp;quot;.device = &amp;quot;/dev/disk/by-uuid/82144284-cf1d-4d65-9999-2e7cdc3c75d4&amp;quot;;&#xA;&#xA;  fileSystems.&amp;quot;/home&amp;quot; = {&#xA;    device = &amp;quot;/dev/disk/by-uuid/b79d3c8b-d511-4d66-a5e0-641a75440ada&amp;quot;;&#xA;    fsType = &amp;quot;btrfs&amp;quot;;&#xA;    options = [&amp;quot;subvol=home&amp;quot;];&#xA;  };&#xA;&#xA;  fileSystems.&amp;quot;/nix&amp;quot; = {&#xA;    device = &amp;quot;/dev/disk/by-uuid/b79d3c8b-d511-4d66-a5e0-641a75440ada&amp;quot;;&#xA;    fsType = &amp;quot;btrfs&amp;quot;;&#xA;    options = [&amp;quot;subvol=nix&amp;quot;];&#xA;  };&#xA;&#xA;  fileSystems.&amp;quot;/persist&amp;quot; = {&#xA;    device = &amp;quot;/dev/disk/by-uuid/b79d3c8b-d511-4d66-a5e0-641a75440ada&amp;quot;;&#xA;    fsType = &amp;quot;btrfs&amp;quot;;&#xA;    options = [&amp;quot;subvol=persist&amp;quot;];&#xA;    neededForBoot = true; # &amp;lt;- add this&#xA;  };&#xA;&#xA;  fileSystems.&amp;quot;/var/log&amp;quot; = {&#xA;    device = &amp;quot;/dev/disk/by-uuid/b79d3c8b-d511-4d66-a5e0-641a75440ada&amp;quot;;&#xA;    fsType = &amp;quot;btrfs&amp;quot;;&#xA;    options = [&amp;quot;subvol=log&amp;quot;];&#xA;    neededForBoot = true; # &amp;lt;- add this&#xA;  };&#xA;&#xA;  fileSystems.&amp;quot;/boot&amp;quot; = {&#xA;    device = &amp;quot;/dev/disk/by-uuid/FDED-3BCF&amp;quot;;&#xA;    fsType = &amp;quot;vfat&amp;quot;;&#xA;  };&#xA;&#xA;  swapDevices = [&#xA;    {device = &amp;quot;/dev/disk/by-uuid/0d1fc824-623b-4bb8-bf7b-63a3e657889d&amp;quot;;}&#xA;    # if you encrypt your swap, it&#39;ll also need to be configured here&#xA;  ];&#xA;&#xA;  nixpkgs.hostPlatform = lib.mkDefault &amp;quot;x86_64-linux&amp;quot;;&#xA;  powerManagement.cpuFreqGovernor = lib.mkDefault &amp;quot;powersave&amp;quot;;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;Do keep in mind that the NixOS hardware scanner &lt;strong&gt;cannot&lt;/strong&gt; pick up your mount&lt;br&gt;&#xA;options. Which means that you should specifiy the options (i.e &lt;code&gt;noatime&lt;/code&gt;) for&lt;br&gt;&#xA;each btrfs volume that you have created in &lt;code&gt;hardware-configuration.nix&lt;/code&gt;. You&lt;br&gt;&#xA;can simply add them in the &lt;code&gt;options = [ ]&lt;/code&gt; list in quotation marks. I&lt;br&gt;&#xA;recommend adding at least zstd compression, and optionally &lt;code&gt;noatime&lt;/code&gt;.&lt;/p&gt;&#xA;&lt;h3 id=&#34;closing-notes&#34;&gt;Closing Notes&lt;/h3&gt;&#xA;&lt;p&gt;And that should be all. By this point you are pretty much ready to install&lt;br&gt;&#xA;with your existing config. I generally use my configuration flake to boot, so&lt;br&gt;&#xA;there is no need to make any revisions. If you are starting from scratch, you&lt;br&gt;&#xA;may consider tweaking your configuration.nix before you install the system.&lt;br&gt;&#xA;An editor, such as Neovim, or your preferred DE/wm make good additions to your&lt;br&gt;&#xA;configuration.&lt;/p&gt;&#xA;&lt;p&gt;Once it&#39;s all done, take a deep breath and &lt;code&gt;nixos-install&lt;/code&gt;. Once the&lt;br&gt;&#xA;installation is done, you&#39;ll be prompted for the root password and after that&lt;br&gt;&#xA;you can reboot. Now you are running NixOS on an encrypted disk. Nice!&lt;/p&gt;&#xA;&lt;p&gt;Next up, if you are feeling &lt;em&gt;really&lt;/em&gt; fancy today, is to configure disk&lt;br&gt;&#xA;erasure and impermanence.&lt;/p&gt;&#xA;&lt;h4 id=&#34;impermanence&#34;&gt;Impermanence&lt;/h4&gt;&#xA;&lt;p&gt;For BTRFS snapshots, I use a systemd service that goes&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;boot.initrd.systemd = {&#xA;  enable = true; # this enabled systemd support in stage1 - required for the below setup&#xA;  services.rollback = {&#xA;    description = &amp;quot;Rollback BTRFS root subvolume to a pristine state&amp;quot;;&#xA;    wantedBy = [&#xA;      &amp;quot;initrd.target&amp;quot;&#xA;    ];&#xA;&#xA;    after = [&#xA;      # LUKS/TPM process&#xA;      &amp;quot;systemd-cryptsetup@enc.service&amp;quot;&#xA;    ];&#xA;&#xA;    before = [&#xA;      &amp;quot;sysroot.mount&amp;quot;&#xA;    ];&#xA;&#xA;    unitConfig.DefaultDependencies = &amp;quot;no&amp;quot;;&#xA;    serviceConfig.Type = &amp;quot;oneshot&amp;quot;;&#xA;    script = &#39;&#39;&#xA;      mkdir -p /mnt&#xA;&#xA;      # We first mount the btrfs root to /mnt&#xA;      # so we can manipulate btrfs subvolumes.&#xA;      mount -o subvol=/ /dev/mapper/enc /mnt&#xA;&#xA;      # While we&#39;re tempted to just delete /root and create&#xA;      # a new snapshot from /root-blank, /root is already&#xA;      # populated at this point with a number of subvolumes,&#xA;      # which makes `btrfs subvolume delete` fail.&#xA;      # So, we remove them first.&#xA;      #&#xA;      # /root contains subvolumes:&#xA;      # - /root/var/lib/portables&#xA;      # - /root/var/lib/machines&#xA;&#xA;      btrfs subvolume list -o /mnt/root |&#xA;        cut -f9 -d&#39; &#39; |&#xA;        while read subvolume; do&#xA;          echo &amp;quot;deleting /$subvolume subvolume...&amp;quot;&#xA;          btrfs subvolume delete &amp;quot;/mnt/$subvolume&amp;quot;&#xA;        done &amp;amp;&amp;amp;&#xA;        echo &amp;quot;deleting /root subvolume...&amp;quot; &amp;amp;&amp;amp;&#xA;        btrfs subvolume delete /mnt/root&#xA;      echo &amp;quot;restoring blank /root subvolume...&amp;quot;&#xA;      btrfs subvolume snapshot /mnt/root-blank /mnt/root&#xA;&#xA;      # Once we&#39;re done rolling back to a blank snapshot,&#xA;      # we can unmount /mnt and continue on the boot process.&#xA;      umount /mnt&#xA;    &#39;&#39;;&#xA;  };&#xA;};&#xA;&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;blockquote&gt;&#xA;&lt;p&gt;You may opt in for &lt;code&gt;boot.initrd.postDeviceCommands = lib.mkBefore &#39;&#39;&lt;/code&gt;&lt;br&gt;&#xA;as &lt;a href=&#34;https://mt-caret.github.io/blog/posts/2020-06-29-optin-state.html&#34;&gt;this blog post&lt;/a&gt;&lt;br&gt;&#xA;suggests. I am not exactly sure how exactly those options actually&lt;br&gt;&#xA;compare, however, a systemd service means it will be accessible through the&lt;br&gt;&#xA;the systemd service interface, which is why I opt-in for a service.&lt;/p&gt;&#xA;&lt;/blockquote&gt;&#xA;&lt;h5 id=&#34;implications&#34;&gt;Implications&lt;/h5&gt;&#xA;&lt;p&gt;What this implies is that certain files such as saved networks for&lt;br&gt;&#xA;network-manager will be deleted on each reboot. While a little clunky,&lt;br&gt;&#xA;&lt;a href=&#34;https://github.com/nix-community/impermanence&#34;&gt;Impermanence&lt;/a&gt; is a great&lt;br&gt;&#xA;solution to our problem.&lt;/p&gt;&#xA;&lt;p&gt;Impermanence exposes to our system an &lt;code&gt;environment.persistence.&amp;quot;&amp;lt;dirName&amp;gt;&amp;quot;&lt;/code&gt; option that we can use to make certain directories or files permanent.&lt;br&gt;&#xA;My module goes like this:&lt;/p&gt;&#xA;&lt;pre&gt;&lt;code class=&#34;language-nix&#34;&gt;imports = [inputs.impermanence.nixosModules.impermanence]; # the import will be different if flakes are not enabled on your system&#xA;&#xA;environment.persistence.&amp;quot;/persist&amp;quot; = {&#xA;  directories = [&#xA;    &amp;quot;/etc/nixos&amp;quot;&#xA;    &amp;quot;/etc/NetworkManager/system-connections&amp;quot;&#xA;    &amp;quot;/etc/secureboot&amp;quot;&#xA;    &amp;quot;/var/db/sudo&amp;quot;&#xA;  ];&#xA;&#xA;  files = [&#xA;    &amp;quot;/etc/machine-id&amp;quot;&#xA;&#xA;    # ssh stuff&#xA;    &amp;quot;/etc/ssh/ssh_host_ed25519_key&amp;quot;&#xA;    &amp;quot;/etc/ssh/ssh_host_ed25519_key.pub&amp;quot;&#xA;    &amp;quot;/etc/ssh/ssh_host_rsa_key&amp;quot;&#xA;    &amp;quot;/etc/ssh/ssh_host_rsa_key.pub&amp;quot;&#xA;    # if you use docker or LXD, also persist their directories&#xA;  ];&#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt;&#xA;&lt;p&gt;And that is pretty much it. If everything went well, you should now be telling&lt;br&gt;&#xA;your friends about your new system boasting full disk encryption &lt;em&gt;and&lt;/em&gt; root&lt;br&gt;&#xA;rollbacks.&lt;/p&gt;&#xA;&lt;h2 id=&#34;why&#34;&gt;Why?&lt;/h2&gt;&#xA;&lt;p&gt;Honestly, why not?&lt;/p&gt;&#xA;&lt;div class=&#34;footnotes&#34; role=&#34;doc-endnotes&#34;&gt;&#xA;&lt;hr&gt;&#xA;&lt;ol&gt;&#xA;&lt;li id=&#34;fn:1&#34;&gt;&#xA;&lt;p&gt;I could be using &lt;code&gt;tmpfs&lt;/code&gt; for &lt;code&gt;/&lt;/code&gt; at this point in time. Unfortunately, since I share this setup on some of my low-end laptops, I&#39;ve got no RAM&lt;br&gt;&#xA;to spare - which is exactly why I have opted out with BTRFS. It is a reliable filesystem that I am used to, and it allows for us to use a script&lt;br&gt;&#xA;that we&#39;ll see later on.&amp;#160;&lt;a href=&#34;#fnref:1&#34; class=&#34;footnote-backref&#34; role=&#34;doc-backlink&#34;&gt;&amp;#x21a9;&amp;#xfe0e;&lt;/a&gt;&lt;/p&gt;&#xA;&lt;/li&gt;&#xA;&lt;li id=&#34;fn:2&#34;&gt;&#xA;&lt;p&gt;&lt;a href=&#34;https://opensource.com/article/20/6/linux-noatime&#34;&gt;https://opensource.com/article/20/6/linux-noatime&lt;/a&gt;&amp;#160;&lt;a href=&#34;#fnref:2&#34; class=&#34;footnote-backref&#34; role=&#34;doc-backlink&#34;&gt;&amp;#x21a9;&amp;#xfe0e;&lt;/a&gt;&lt;/p&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ol&gt;&#xA;&lt;/div&gt;&#xA;</description>
      <pubDate>Tue, 14 Mar 2023 00:00:00 +0000</pubDate>
    </item>
    <item>
      <title>system-backlight</title>
      <link>system-backlight.html</link>
      <description>&lt;h1 id=&#34;notes-for-22th-of-january-2023&#34;&gt;Notes for 22th of January, 2023&lt;/h1&gt;&#xA;&lt;p&gt;Following a system upgrade two days ago, my HP Pavillion laptop has stopped&lt;br&gt;&#xA;registering the &lt;code&gt;intel_backlight&lt;/code&gt; interface in &lt;code&gt;/sys/class/backlight&lt;/code&gt;, which&lt;br&gt;&#xA;is most often used to control backlight by tools such as &lt;code&gt;brightnessctl.&lt;/code&gt;&lt;br&gt;&#xA;Inspecting &lt;code&gt;dmesg&lt;/code&gt; has given me nothing but aninsanely vague error message.&lt;br&gt;&#xA;Only mentioning it is not being loaded (&lt;em&gt;very helpful, thanks!&lt;/em&gt;)&lt;/p&gt;&#xA;&lt;p&gt;After some research, on Google as every other confused Linux user, I have&lt;br&gt;&#xA;come across &lt;a href=&#34;https://www.linuxquestions.org/questions/slackware-14/brightness-keys-not-working-after-updating-to-kernel-version-6-a-4175720728/&#34;&gt;this article&lt;/a&gt;&lt;br&gt;&#xA;which mentions backlight behaviour has changed sometime after kernel 6.1.4.&lt;br&gt;&#xA;Fortunately for me, the article also refers to the the ever so informative&lt;br&gt;&#xA;ArchWiki, which instructs passing one of the &lt;a href=&#34;https://wiki.archlinux.org/title/backlight#Kernel_command-line_options&#34;&gt;three kernel command-line options&lt;/a&gt;.&lt;br&gt;&#xA;depending on our needs.&lt;/p&gt;&#xA;&lt;p&gt;As I have upgraded from 6.1.3 to 6.1.6 with a flake update, the &lt;code&gt;acpi_backlight=none&lt;/code&gt;&lt;br&gt;&#xA;parameter has made it so that it would skip loading intel backlight entirely. Simply switching&lt;br&gt;&#xA;this parameter to &lt;code&gt;acpi_backlight=native&lt;/code&gt; as per the article above has fixed the issue.&lt;/p&gt;&#xA;</description>
      <pubDate>Sun, 22 Jan 2023 00:00:00 +0000</pubDate>
    </item>
  </channel>
</rss>